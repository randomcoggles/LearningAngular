import { Injectable } from '@angular/core';
import { Observable, of } from 'rxjs';

@Injectable({
  providedIn: 'root'
})
export class MenuService {

constructor() { }

  // list(): Observable<MenuItem[]> { // TODO: to promise
  //   return of([]);
  // }

}
